django>=4.2,<5
weasyprint>=58.0
Pillow>=10.0
reportlab>=4.0
# Note: WeasyPrint needs system libraries (cairo, pango). On Windows use the wheels or follow WeasyPrint docs.
# Pillow and reportlab are pure-python and commonly available via pip.